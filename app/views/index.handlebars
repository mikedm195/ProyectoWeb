<div class="container">
<p>{{msg}}</p>
    <h1>Examen/Proyecto Final</h1>
  {{#if session}}
    <h1><img src="/uploads/{{image}}" width="200px" height="150px"/> Bienvenido {{session}} </h1>
    
    <a href="/users/editarUsuario" class="btn btn-primary">Editar Usuario</a><br/>
  {{/if}}
</div>
